[project]
name = "llm-data"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "daft[ray]>=0.7.16",
    "ftfy>=6.3.1",
    "sentence-transformers>=5.6.0",
]
